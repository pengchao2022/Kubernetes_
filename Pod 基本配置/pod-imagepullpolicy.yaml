
apiversion: v1
kind: Pod
metadata:
  name: pod-imagepullpolicy
  namespace: dev2025
spec:
  containers:
  - name: nginx
    image: nginx:1.71.1
    imagePullPolicy: Always #始终从远程仓库拉去镜像
  - name: busybox
    image: busybox:1.30
    imagePullPolicy: ifNotPresent #本地有的话使用本地的，本地没有的话，从远程仓库拉去

