apiVersion: v1
kind: Service
metadata:
  name: svc-nginx
  namespace: dev2025
spec:
  ClusterIP: 10.109.179.231 #也可以不指定IP
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    run: nginx
  type: ClusterIP
